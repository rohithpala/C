// #include<stdio.h>
// #include<stdlib.h>
// #include "P:\C\Sortings.h"
// #define MAX_SIZE 10

// typedef struct FileTree{
//      int wt;
//      struct FileTree *left_child, *right_child;
// } FileTree;

// // FileTree* newFile(int wt){
// //      FileTree* tempFile = (FileTree*)malloc(sizeof(FileTree));
// //      tempFile->wt = wt;
// //      tempFile->left_child = tempFile->right_child = NULL;
// //      return tempFile;
// // }

// void main()
// {
//      int i, j, n, totalRecordMoves = 0;
//      printf("Enter Number of Files: ");
//      scanf("%d", &n);
//      FileTree *rootFile = NULL;
//      printf("Enter Sizes of %d Files: ", n);
//      for (i = 0 ; i < n ; i++){}
//      // for (i = 0 ; i < n ; i++){
//      //      scanf("%d", &size[i]);
//      // }
//      // MergeSort(size, 0, n-1, 'f');
//      // for(i = 0 ; i < sizeof(size)/sizeof(size[0]) ; i++){
//      //      heap->left_child = size[i];
//      //      heap->right_child = size[i+1];
//      //      heap->wt = heap->left_child->wt + heap->right_child->wt;
//      //      size = (int*)realloc(size, n + 1);
//      //      size[n+i] = heap->wt;
//      //      InsertionSort(size, n);
//      //      totalRecordMoves += heap->wt;
//      // }
//      // printf("Total Number of Record Moves: %d", totalRecordMoves);
//      // free(size);
//      // for (i = 1 ; i < n ; i++){
//      //      for (j = 0 ; j < i+1 ; j++){
//      //           totalRecordMoves += size[j]; // Check for 2 3 4 5 6 7
//      //      }
//      // }
// }
